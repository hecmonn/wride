import React, { PropTypes } from 'react'

class Feed extends React.Component {
    constructor(props){
        super(props);
    }
    render () {
        return(
            
        )
    }
}

export default Feed;
